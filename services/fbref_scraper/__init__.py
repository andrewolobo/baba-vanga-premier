"""Fixture scraping from fbref date pages (docs/FBREF_SCRAPER.md).

One request per calendar date returns every competition worldwide, so a week of
English fixtures costs seven requests rather than one per league.
"""
