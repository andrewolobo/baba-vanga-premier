# docs/SEO_PLAN.md 1.1, 2.7. A keep-out list for crawlers, not a list of
# content: that is the sitemap below.
#
# /api/ is deliberately NOT disallowed. The rule was written while pages were
# client-rendered and a crawler had to fetch /api/tips to see a call; since
# SEO_PLAN.md 2.1 the calls are in the HTML, but a disallow still gains
# nothing: keeping /api/ responses out of the index is a response header's
# job (SEO_PLAN.md 1.3), and a crawler must be able to fetch a URL to read it.
User-agent: *
Disallow: /book
Disallow: /performance

Sitemap: https://babavanga.net/sitemap.xml
