# docs/SEO_PLAN.md 1.1. A keep-out list for crawlers, not a list of content.
#
# /api/ is deliberately NOT disallowed. The pages are client-rendered, so a
# crawler that renders them has to fetch /api/tips to see a single call;
# blocking it would get an empty shell indexed. Keeping /api/ responses out
# of the index is a response header's job (SEO_PLAN.md 1.3).
#
# No Sitemap line until a sitemap exists (SEO_PLAN.md 2.7).
User-agent: *
Disallow: /book
Disallow: /performance
